# Source video and archives — never commit (GitHub size limits)
*.mp4
*.MP4
*.zip
source/

# data & artifacts — never commit imagery or weights (frames/ is the
# deliberately tracked exception)
data/
datasets/
raw/
runs/
*.pt
*.onnx
*.engine

# python
.venv/
__pycache__/
*.pyc

# secrets
kaggle.json
